package db_sp_call

const (
	procedureCall = "CALL"
	functionCall  = "SELECT"

	tag                    = "db"
	keyErrorCode           = "error"
	keyErrorMessage        = "message"
	keyErrorAdditionalData = "additional_data"
)
